services:
  - type: web
    name: frontend
    runtime: node
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 24.14.1
      - key: NEXT_PUBLIC_API_URL
        value: https://productsearch-backend-dev.onrender.com/api